define([],

  function () {

    'use strict';

    var globals = {};

    globals.variables = {
      files : null,
      docsCount : 0
    };

    globals.constants = {};

    globals.events = {
      BITMAP_CHANGE : 'onBitmapChanged'
    };

    globals.utils = {};

    return globals;

  });
